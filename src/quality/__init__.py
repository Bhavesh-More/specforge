"""Cloud-quality execution helpers for SpecForge.

Keep this package initializer intentionally light: core template models import
``src.quality.models``, so importing orchestrator objects here would create a
cycle during application startup.
"""
